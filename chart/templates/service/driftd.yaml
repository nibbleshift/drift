apiVersion: v1
kind: Service
metadata:
  name: {{ include "drift.fullname" . }}
  labels:
    {{- include "drift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drift.selectorLabels" . | nindent 4 }}
