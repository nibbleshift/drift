version: '3'

tasks:
  default:
    cmds:
      - task: generate
      - task: build-driftd
      - task: build-driftctl
  generate:
    run: when_changed
    cmds:
      - go generate .
    sources:
      - ent/schema/*.go
      - gqlgen.yml
  build-driftd:
    run: when_changed
    deps: [generate]
    sources:
      - cmd/driftd/*.go
      - ent/*.go
    generates:
      - driftd
    cmds:
      - go mod tidy
      - go build -o driftd cmd/driftd/*.go 
  build-driftctl:
    run: when_changed
    deps: [generate]
    sources:
      - cmd/driftctl/*.go
      - ent/*.go
    generates:
      - driftctl
    cmds:
      - go mod tidy
      - go build -o driftctl cmd/driftctl/*.go 
