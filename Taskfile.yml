version: '3'

tasks:
  default:
    cmds:
      - task: generate
      - task: build-driftd
      - task: build-driftctl
  generate:
    run: when_changed
    cmds:
      - go generate .
      - go mod tidy
    sources:
      - ent/schema/*.go
      - gqlgen.yml
  build-driftd:
    run: when_changed
    deps: [generate]
    sources:
      - cmd/driftd/*.go
      - ent/*.go
      - ./*.go
    generates:
      - driftd
    cmds:
      - go build -o driftd cmd/driftd/*.go 
  build-driftctl:
    run: when_changed
    deps: [generate]
    sources:
      - cmd/driftctl/*.go
      - ent/*.go
      - ./*.go
    generates:
      - driftctl
    cmds:
      - go build -o driftctl cmd/driftctl/*.go 
